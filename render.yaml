services:
  - type: web
    name: medbot-backend
    runtime: node
    rootDir: backend
    buildCommand: npm install
    startCommand: npm run start
    envVars:
      - key: NODE_VERSION
        value: "22"
      - key: PORT
        value: "5001"
      - key: NODE_ENV
        value: production
